package com.lld.app.registerCallback.client;

/**
 * Callback interface
 * This is the client callback function interface which is registered at the registry class.
 */
public interface ICallback {
    void callback();
}
